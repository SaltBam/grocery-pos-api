services:
  mongodb:
    image: mongo:latest
    container_name: grocery-mongodb
    ports:
      - '27017:27017'
    volumes:
      - mongodb_data:/data/db
    command: mongod --replSet rs0 --bind_ip_all

volumes:
  mongodb_data:
